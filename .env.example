# Environment Variables for MOTIVEX Production

# ============================================================================
# DATABASE
# ============================================================================
# Supabase PostgreSQL via PgBouncer (Pooler)
# CRITICAL: Use pooler on port 6543 with these exact parameters to prevent
# "prepared statement does not exist" errors
DATABASE_URL="postgresql://USER:PASSWORD@HOST:6543/DATABASE?pgbouncer=true&connection_limit=1&statement_cache_size=0"

# For Prisma schema validation (even though 5432 might be blocked)
# Set this to the same as DATABASE_URL or use the direct URL if accessible
DIRECT_URL="postgresql://USER:PASSWORD@HOST:6543/DATABASE?pgbouncer=true&connection_limit=1&statement_cache_size=0"

# ============================================================================
# AUTHENTICATION
# ============================================================================
# NextAuth v5 Configuration
NEXTAUTH_SECRET="your-super-secret-key-generate-with-openssl-rand-base64-32"
NEXTAUTH_URL="https://motivex.app"

# Production site URL for metadata/SEO (set in production)
NEXT_PUBLIC_SITE_URL="https://motivex.app"

# ============================================================================
# ADMIN BOOTSTRAP (Production Only)
# ============================================================================
# Admin account created automatically on first deployment
# CRITICAL: Set these in Vercel environment variables ONLY
ADMIN_EMAIL="owner@motivex.dz"
ADMIN_PASSWORD="your-strong-admin-password-change-immediately"

# ============================================================================
# OPTIONAL: Email & Notifications
# ============================================================================
# RESEND_API_KEY="re_..."

# ============================================================================
# OPTIONAL: Rate Limiting (Production)
# ============================================================================
# UPSTASH_REDIS_URL="..."
# UPSTASH_REDIS_TOKEN="..."

# ============================================================================
# NOTES
# ============================================================================
# 1. DATABASE_URL must include: pgbouncer=true & connection_limit=1
# 2. DIRECT_URL can be same as DATABASE_URL if port 5432 is blocked
# 3. Prisma migrations must be run via Supabase SQL Editor (manual)
# 4. Never commit this file to version control (.env in .gitignore)
#
# To generate NEXTAUTH_SECRET:
#   openssl rand -base64 32
